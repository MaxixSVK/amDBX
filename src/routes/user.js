const express = require('express');
const router = express.Router();
const User = require('../models/user');

router.post('/user/anime/progress', async function (req, res) {
});
    module.exports = router;